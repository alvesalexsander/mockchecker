{
  "name": "mockchecker",
  "version": "1.0.0",
  "description": "Toolkit to develop your own mock verifier application.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-dev": "tsc -w",
    "build": "tsc && prettier --write \"src/**/*.ts\" \"dist/**/*.js\"",
    "format": "prettier --write \"src/**/*.ts\" \"dist/**/*.js\"",
    "lint": "tslint -p tsconfig.json"
  },
  "author": "Alexsander Alves",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.0.27",
    "chalk": "^4.1.0",
    "jest": "^26.1.0",
    "moment": "^2.27.0",
    "prettier": "^2.0.5",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.9.7"
  }
}
